<form method="post" action="{{ path('reponses_delete', {'id': reponse.id}) }}" onsubmit="return confirm('Êtes vous sûr de supprimer?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reponse.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
